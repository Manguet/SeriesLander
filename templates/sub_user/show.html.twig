{% extends 'base.html.twig' %}

{% block title %}SubUser{% endblock %}

{% block body %}
    <h1>SubUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sub_user.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sub_user.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sub_user_index') }}">back to list</a>

    <a href="{{ path('sub_user_edit', {'id': sub_user.id}) }}">edit</a>

    {{ include('sub_user/_delete_form.html.twig') }}
{% endblock %}
